using UnityEngine;
using System.Collections;

public class GrenadeMove : MonoBehaviour 
{
	public float gr_speed;
	Rigidbody rb;
	
	// Update is called once per frame
	void Update () 
	{
		//rb.AddForce(Vector3.forward * gr_speed* Time.deltaTime);
		//transform.Translate(0, 0, gr_speed);
	}



}
